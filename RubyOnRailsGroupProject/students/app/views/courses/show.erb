<p>
  <strong>Course Number:</strong>
  <%= @course.c_number %>
</p>

<p>
  <strong>Course Name:</strong>
  <%= @course.c_name %>
</p>

<p>
  <strong>Course Hours:</strong>
  <%= @course.c_hours %>
</p>

<h2> Comments </h2>
<%= render @course.comments %>

<h2>Add a comment: </h2>
<%= render 'comments/form' %>


<%= link_to 'Edit Course', edit_course_path(@course), class: 'btn btn-primary btn-sm' %>
<%= link_to 'Back to Courses', courses_path(@course), class: 'btn btn-info btn-sm' %>

